import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images:{
  domains:['pokeapi.co','i.pinimg.com','i.pinimg.com','raw.githubusercontent.com']
  }
};

export default nextConfig;
